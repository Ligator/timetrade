<%= link_to service_path(service), class: "link-card card h-100 text-decoration-none text-dark text-justify" do %>
  <div class="card-body">
    <h5><%= service.title.truncate(50) %></h5>
    <p class="text-muted"><%= service.description.truncate(300) %></p>
  </div>
<% end %>
